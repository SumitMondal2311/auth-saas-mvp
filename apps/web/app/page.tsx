import { Metadata } from "next";

export const metadata: Metadata = {
    title: "auth-saas | Manage authentication and user with ease",
};

export default function LandingPage() {
    return <div className="flex h-screen items-center justify-center">Landing Page</div>;
}
